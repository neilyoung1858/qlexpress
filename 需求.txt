我想要使用antv\x6 4.1+react做一个画布用来给信用卡积分生成qlexpress表达式使用
画布只要画好流程就行，提交生成的json,然后后端java(springboot)项目会把这个转成ql表达式
主要是给业务人员使用，我想要它操作简单同时可读性还要强，业务人员能快速上手操作，流程清晰
最好使用拖拽的方式就能完成


面向业务人员：AntV X6 积分规则画布极简设计方案（低门槛、强可读、零代码）
 
核心设计原则（完全贴合业务运营/产品，不用懂代码）
 
1. 零代码拖拽：纯可视化，不写任何QL、不用懂语法；

2. 语义化中文节点：不出现「条件判断、表达式、运算符」等技术词；

3. 流程天然自上而下：单一主线+分支分叉，无交叉、无循环，一眼看懂逻辑；

4. 实时预览QL脚本：业务只看流程图，技术看自动生成的表达式；

5. 内置模板：信用卡积分高频规则预制，点选即用，少手动输入；

6. 自带模拟试算：填一笔交易，实时出积分，业务当场验证规则对错。
 
一、画布整体布局（页面三分栏，清晰不混乱）
 
左侧：组件拖拽栏（中文大白话分类，无专业术语）
 
分4大模块，每个图标+中文名称，业务一眼知道用途
 
1. 流程起点（仅1个，自动固定画布顶部）
 
【开始-交易积分核算】
 
2. 分支判断模块（业务核心，替代if/else）
 
- 【交易条件分流】：按交易信息区分规则（金额、商户MCC、卡种、用户等级、节假日、是否退货/分期）
 
3. 积分执行模块（满足条件后做什么）
 
- 【多倍积分计算】：1倍/2倍/5倍、设置单笔积分上限

- 【无积分拦截】：批发、房产、购车、公益类不计积分

- 【积分扣减】：退货、分期手续费扣减积分
 
4. 流程终点（每个分支必须落地）
 
【输出最终积分】
 
中间：画布操作区（核心操作区）
 
1. 强制规范：

- 只能从上往下连线，禁止逆向连线、交叉连线；

- 一个【交易条件分流】节点最多4条分支（if / elif1 / elif2 / else），分支太多业务会乱，复杂规则拆两张画布；

2. 连线标签（可读性关键）：
拉出连线后弹窗填写中文分支说明，例如：「分支1：MCC=餐饮/超市」「分支2：MCC=房产批发」「其他所有交易」；
连线上方永久展示文字，不用点开节点就能看懂分支含义。

3. 画布辅助功能：

- 自动排版：一键规整所有节点，整齐对齐，不会拖拽得乱七八糟；

- 注释框：可拖拽文本框写业务备注（例如：2026暑期餐饮5倍活动）；

- 撤销/重做、复制整条分支（活动规则复用神器）。
 
右侧：属性配置+实时预览双标签
 
标签1：节点配置面板（表单化，下拉选择，极少手动输入）
 
拒绝自由输入，能下拉绝不手填，减少业务输错：
 
1. 条件节点配置示例（交易条件分流）
校验对象（下拉）：交易金额、商户MCC、卡片类型、用户等级、交易日期、是否退货
判断关系（下拉）：大于、小于、等于、包含、属于指定列表、不等于
匹配值：

- MCC/卡种：下拉预设选项（餐饮5812、超市5411、白金卡、普通卡…）

- 金额：数字输入框

2. 积分计算节点
基础积分比例：下拉 1元1分 / 10元1分
积分倍率：下拉 1倍/2倍/3倍/5倍/0倍
单笔积分封顶：数字输入（可不填，代表无上限）

3. 无积分拦截：仅需勾选拦截原因（批发商户、购房购车、公益缴费、退货交易）
 
标签2：QL表达式实时预览（只读，业务不用管，给技术核对）

适配动态自定义字段：面向业务的X6积分画布完整方案
 
核心诉求：字段不固定、业务可随时新增交易/用户/活动自定义字段，全程不写代码，自动兼容QLExpress生成，兼顾可读性与扩展性。
 
一、整体分层改造（新增【自定义字段管理】模块，全局统一）
 
1. 顶部全局：自定义字段管理入口（所有画布共用字段池）
 
单独弹窗管理字段，业务运营自行新增、编辑、停用字段，全局复用，不用每个规则单独建：
 
字段新增表单（极简表单，无技术术语）
 
1. 字段展示名称（业务可读）：如「活动专属消费门槛」「当月累计消费金额」「是否新户」「活动档位」

2. 字段标识（后端QL变量名，自动生成，业务不用改）
输入展示名自动转驼峰英文，例：活动专属消费门槛 → activityThreshold；支持手动微调，限制英文+数字下划线

3. 字段数据类型（下拉选择，决定运算符/输入组件）

- 数字型：金额、积分、累计次数、档位数值（支持 > < >= <= == !=）

- 文本枚举型：卡等级、活动标签、商户大类（支持 == != in not in）

- 布尔型：是/否（新户、分期、参与活动）（支持 == true / == false）

- 日期型：交易日期、活动到期日（支持大于小于日期）

4. 可选预设枚举值（可选）
文本类型可批量录入选项：如新户、老户、钻石用户；后续节点配置直接下拉，不用手动输入。

5. 字段用途标签：交易信息 / 用户信息 / 活动自定义参数（仅用于分类筛选）
 
字段池能力
 
- 新增字段实时同步所有画布；

- 停用字段：画布内标灰提示，旧规则可保留，新建规则不可选；

- 字段引用统计：查看哪些积分规则用到该字段，删除前校验依赖。
 
2. 左侧拖拽组件区（无改动，仅配置面板联动全局字段池）
 
流程起点、交易条件分流、积分计算、无积分拦截、结束节点保持大白话，不增加学习成本。
 
3. 中间画布区（无改动，保持简单线性分支，可读性不变）
 
约束：单层分支、连线带中文说明、禁止交叉/循环，降低业务理解成本。
 
4. 右侧配置面板（核心改造：条件节点自动加载全局自定义字段）
 
场景1：【交易条件分流】节点配置（动态字段下拉）
 
1. 【判断对象】下拉框 = 全局字段池所有启用字段（区分系统内置+自定义，分组展示）

- 系统内置字段：tradeAmt交易金额、mcc商户码、cardType卡种（固定存在，不可删除）

- 自定义活动字段：业务自己新增的全部字段，单独分组，一眼区分

2. 选中字段后，自动根据字段类型过滤可用运算符（杜绝错误搭配）
例：布尔字段只显示【是/否】；数字字段显示大于、小于、等于；枚举文本显示属于列表、不等于

3. 匹配值输入组件自适应字段类型

- 数字：数字输入框

- 枚举文本：多选下拉框（预填业务录入的枚举）

- 布尔：单选框 是/否

- 无自由文本输入，减少输错
 
场景2：【多倍积分计算】节点支持自定义字段参与运算
 
业务计算积分时，不仅能写固定数字，还能选择自定义字段作为动态参数：
示例场景：
 
- 活动档位倍率：自定义字段 activityRate ，满足条件时 point = basePoint * activityRate

- 活动封顶上限：自定义字段 activityMaxPoint ，point = min(point, activityMaxPoint)
运算下拉选项：固定数值 / 全局自定义字段
 
二、动态字段 → QLExpress 自动映射规则（完全自动，业务无感知）
 
1. 变量规范
 
- 系统内置字段：直接原生变量  tradeAmt、mcc 

- 业务自定义字段：统一作为上下文变量注入QL引擎，直接使用标识名
例：自定义字段「活动档位倍率」标识 activityRate ，QL中直接写  activityRate 
 
2. 类型自动适配转换，无需人工处理
 
表格
   
画布配置（自定义字段） 自动生成QL表达式 
自定义数字字段【活动门槛】> 3000 activityThreshold > 3000 
自定义布尔【是否活动新户】等于是 isNewUser == true 
自定义枚举【活动档位】属于 [档位A,档位B] in(activityLevel,["档位A","档位B"]) 
积分倍率使用自定义字段 point = basePoint * activityRate 
封顶使用自定义字段 point = min(point, activityMaxPoint) 
 
3. 执行层兼容改造（后端）
 
1. 积分核算接口统一入参Map，同时传入系统内置交易参数 + 当前活动所有自定义字段参数

2. QLExpress上下文一次性装载全部字段，表达式直接读取，无需修改引擎

3. 新增字段不用改后端代码，前端字段池新增即可，完全动态扩展
 
三、业务操作完整流程（新增自定义字段→配置规则，全程简单）
 
步骤1：业务新增活动专属自定义字段（5秒完成）
 
页面顶部点击「管理活动字段」→ 新增字段 → 填写名称、选择类型、录入枚举（可选）→ 保存，自动全局生效。
示例：本次暑期活动需要「暑期消费档位、暑期倍率、暑期单笔积分上限」，一次性新增3个字段。
 
步骤2：画布搭建积分规则，直接选用自定义字段
 
拖拽【交易条件分流】，判断对象下拉找到「活动自定义字段」分组，选中刚才新增的档位字段，配置条件。
 
步骤3：积分计算节点引用自定义字段做动态倍率/封顶
 
多倍积分配置页面，倍率选择「自定义字段-暑期倍率」，封顶选择「自定义字段-暑期单笔积分上限」。
 
步骤4：模拟测试自动填充自定义字段值
 
右侧测试面板同步渲染所有字段输入框：
 
- 系统内置：交易金额、MCC

- 自定义新增：暑期消费档位、暑期倍率
业务填入本次活动测试值，一键试算，流程图自动高亮命中分支，积分计算明细展示自定义字段参与运算过程。
 
步骤5：保存规则
 
同时存储三份数据：
 
1. X6画布JSON（包含节点引用的字段ID，回显自动加载自定义字段）

2. 全局字段快照（规则绑定创建时的字段定义，后续字段修改不影响历史旧规则）

3. 自动生成完整QLExpress表达式（动态包含自定义字段变量）
 
四、解决业务痛点设计细节
 
1. 字段隔离，活动互不干扰
 
字段支持标签分类，A活动新增的字段不会干扰B活动画布，下拉分组展示，视觉干净。
 
2. 历史规则兼容（重点）
 
字段修改/新增不会破坏旧规则：保存规则时快照当前字段结构，编辑历史规则时读取快照，就算后续字段改名、新增，旧规则配置不会失效。
 
3. 错误拦截，防止字段逻辑异常
 
画布实时校验：
 
1. 节点引用已停用字段，标红提示：「该字段已停用，请更换判断条件」

2. QL表达式实时语法校验，若自定义字段类型和运算符不匹配，直接弹窗提示，禁止保存

3. 测试时若未传入表达式用到的自定义字段，试算页面提示补全参数
 
4. 可读性保障（业务友好）
 
1. 画布节点悬浮提示：鼠标悬浮条件，展示完整中文逻辑，包含自定义字段中文名
例：判断条件：暑期消费档位 属于 档位A、档位B

2. QL预览脚本增加中文注释，标注每个变量对应的业务字段名

五、拓展高阶场景（适配多变活动需求）
 
1. 临时活动一次性字段
用完可停用，不会污染字段下拉列表；支持批量隐藏历史活动字段。

2. 字段复制复用
相似活动字段一键复制，仅修改名称、数值，不用重复新建。

3. 多字段组合条件
条件节点支持多条件且/或拼接，系统字段+自定义字段混合判断：
例：交易金额>1000 并且 暑期消费档位 == 档位A
QL自动生成：tradeAmt > 1000 && activityLevel == "档位A"
 
六、总结优势
 
1. 字段完全动态扩展，业务任意新增活动参数，无需前端/后端发版改代码；

2. 对业务人员零门槛：新增字段仅简单表单填写，画布配置全部下拉选择，不用写代码；

3. 原有简洁流程架构不变，仅新增全局字段管理，流程图依旧清晰易懂；

4. QL表达式全自动适配自定义字段，技术无需手动维护脚本；

5. 历史规则强兼容，字段变更不会导致旧规则失效，上线无风险；

6. 自带试算功能，业务可填入自定义字段数值，自行验证活动积分逻辑正确性

页面一定要专业好看布局合理
